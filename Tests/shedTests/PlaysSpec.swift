import Quick
import Nimble
@testable import shed

class PlaysSpec : QuickSpec {
  override func spec() {
    describe("play logic") {
      it("fails this test") {
        expect(3).to(equal(2))
      }
    }
  }
}
